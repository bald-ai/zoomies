Implement latest user request, same checkout preserving existing changes, no delegation/branch/worktree/commit. User: Add scaling for text, marker too small on full-screen images. Make marker selected color affect circle outline and number, not background. Interpretation: hollow/transparent interior; chosen color strokes outline and numeral. No filled circle/background. Apply renderer, cursor preview (shared renderer), inline numeral edit color, README and meaningful render tests. Remove obsolete contrast helper if unused, preserve palette helper needed elsewhere.
Sizing: defaultTextFontSize currently round(38.4 / textCreationZoomFactor), marker diameter same. Controller passes only defaultUserZoomFactor, omitting baseScale including Retina/canvas conversion and fit. Pass complete actual initial effective scrollView.magnification AFTER applyZoom; freeze this initial creation scale for session so manual zoom doesn't change subsequently created annotation sizes. New text and markers should appear ~38.4 screen points at initial display across full-screen and small captures. Existing saved annotations retain stored sizes; prompt footer and shape thickness unaffected. Inspect min/max clamps and ImageSafety.maxFontSize to avoid introducing unsafe save sizes. Marker cursor MUST remain matched to placed bounds using live magnification (previous followup fixed this).
Add regression coverage through real EditorWindowController initial setup (or existing test harness) for small and full-screen/large images: text fontSize * initial actual magnification approximately38.4, same marker diameter relation. Ensure manual zoom doesn't reset creation size. Pixel rendering tests verify transparent interior preserves underlying image, chosen color numeral/outline. Run targeted/full tests, hand off promptly. Parent will build/install/relaunch updated app for user to test. Prior run 255 tests passed. Relevant Sources/EditorWindowController.swift sizeWindowToImage ~805, EditorCanvasView.swift sizing and beginEditingMarker, EditorImageRenderer.swift drawMarker and markerContentColor, NumberedMarkerTests and EditorWindowControllerTests. Own Sources, Tests, README per inherited scope. No other changes since last completion.