This is a tiny watcher experiment, not app development. Execute exactly one shell command: python3 -c "import time; time.sleep(12); print(731 * 29)". Then report the observed integer and finish your turn. Do not inspect application code, edit files, install anything, delegate, change branches, or create worktrees. You have a scratch write scope only to satisfy the controller; no files need writing. Do not create any readiness or completion flag. Use the appended handoff format, keeping it brief. This verifies whether your CLI emits completion without a model-written marker.
