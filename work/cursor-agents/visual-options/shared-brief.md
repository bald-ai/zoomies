# Assignment context

The user is not a coder. They liked a screenshot of a zoomable code wall, but disliked examples of CodeCharta's 3D city, CodeScene's bubble maps, and Emerge's tangled networks. They request ONE downloadable, self-contained HTML file offering FIVE distinctly different ways to gain visual clarity about their own codebase at a high level. They explicitly authorized external Cursor workers using the configured Grok model, up to 16. You are one of five workers; do NOT delegate further.

The parent is creating the final navigation and combining five independent concept documents. You own ONLY the concept HTML and notes file named in your individual prompt. Read other project files freely. Preserve all existing user work. Stay on current branch and current checkout. No branches, worktrees, commits, installs, builds of the Swift app, app source changes, network calls, or external messages.

Project: /Users/applelevne/Documents/coding-projects/zoomies. Native Swift/AppKit screenshot + annotation + notes + recording app. Read README.md and relevant Sources files as needed. `work/code-views/facts.json` contains measured file lengths and commit counts: 44 Swift source files; 11,278 total lines including blanks/comments; 19 reachable commits touching Sources. Counts are a snapshot of 2026-09-12. Source file lengths include current local changes, historical counts do not. Tests and resources excluded. This is one Swift application target, so functional groups are inferred roles, not compiler-enforced boundaries.

## Shared output contract

Create a complete standalone HTML document with inline CSS and inline JS, no external assets, fonts, packages, fetches, or dependencies. It will appear inside an iframe of a larger comparison page. Do not duplicate overall page navigation, header, five concept chooser, or footer. Keep your own compact visualization title and one-line question. Make the VISUAL the main content, with working selection/step interactions and an accessible selected-detail area. Target 900px wide, support 340px without horizontal overflow, and keep desktop height around 600-780px where practical. Fit naturally to content, no fixed outer height, no viewport-height rules. Avoid internal scrolling; at narrow widths reflow or stack. Use semantic native buttons, labels, and keyboard focus styles. Use DOM textContent for text. No fake buttons.

Art direction: calm, precise, warm paper + ink, a little editorial, not a developer dashboard. Background #f8f7f3, surfaces #ffffff, text #25352f, secondary #66736b, border #dce2da, green #34664e, orange #b56838, blue #4e6f96, purple #796496. Use system sans-serif, with Georgia only for a short concept heading if it helps. Rounded corners 12-18px, subtle fills, no gradients, no huge hero, no generic KPI card row, no emojis. Render diagrams natively with HTML/CSS/SVG. Text at least 13px; body 15-16px. Be visually distinctive through diagram composition, not unrelated palettes.

Copy: plain English for someone who doesn't code. Name the job first (e.g. Drawing tools), place Swift filenames behind a details disclosure or selected evidence section. Explain the implication, not terminology. Never claim file size equals poor quality. Never invent health scores, coverage, bugs, measured dependencies, change forecasts, or abstraction-depth ratios. All measured numbers must use facts.json or independently verified source. Label manually traced relationships as 'Illustrated from the current code' and qualitative interpretations as 'A question to investigate'. Don't label everything hypothetical: distinguish observed facts from suggested visualization method accurately. Keep the artifact useful for choosing a direction.

At the bottom include two short lines or a compact area: 'Helps you see' and 'Does not tell you' explaining this approach's strengths and limitations. Include an 'Evidence' details disclosure listing actual source paths/line references supporting the diagram and any counting/grouping method. File names are not clickable inside the iframe; text is fine. Do not expose controller/log details in the product.

Parent sizing integration: on load and on any size change, call `parent.postMessage({type:'zoomies-concept-height', id:'YOUR_ID', height:document.documentElement.scrollHeight}, '*')`. Use a ResizeObserver on body and a requestAnimationFrame debounce, plus window load. Parent accepts only active iframe source, and id must equal your concept id. Set html/body margin:0 and min-width:0. Do not otherwise interact with parent.

Write a concise notes file with evidence, interaction descriptions, limitations, and anything the parent must check. In your final result report exact owned files and checks performed. Use macOS-compatible commands. Verify syntax if available; no GNU-only command assumptions.
