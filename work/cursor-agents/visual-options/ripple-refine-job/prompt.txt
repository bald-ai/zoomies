Related follow-up on your completed 03-ripple.html. Parent inspected it in browser at ~900px iframe width. Material UX issue: ALL 12 job detail cards are permanently displayed in the right side. It makes a very tall column with huge blank space under the diagram and overwhelms the noncoding user. Refine your owned HTML and notes only. Keep the three scenario choices, but show ONLY the SELECTED job's detail, default the first directly involved job. Make each job selectable using native HTML buttons (diagram can supplement; keyboard and touch must work). If current concentric diagram with 12 tiny labels is too cramped, replace with a clean three-band diagram / branching lanes with readable 13px+ labels and 3-5 consolidated job groups per scenario. User disliked dense node networks. Keep it visual, NOT a giant list of text cards. Need compact dominant visual, one selected detail; desktop target max~850px document height excluding opened Evidence. Simplify visible language: "Hidden editing data" instead of "PNG text chunks"; "Save the picture" instead of "Save image bytes"; "Bring edits back" instead of "Reopen recovery"; "Prepare a PNG" instead of "Pixel → PNG helper". Move Swift file hints and technical terms iTXt/JSON/bitmaps/payloads into collapsed Evidence. Remove redundant disclaimer paragraphs; one clear "Places to inspect, traced from the code" is enough plus usual limitations. Parent will fix shared height mechanics separately. Preserve verified relationships. Do not edit any other files, delegate, install, or switch git state. Run syntax checks and finish.
