Follow-up for your owned 04-iceberg.html and notes only. Browser visual looks usable, but text is too technical for the user's explicit "I am not a coder". Refine VISIBLE copy with human job language and shorter paragraphs. Keep filenames/JSON/iTXt/checksums/atomic writes/callers/API/persistence in collapsed Evidence only. Above water label "What the rest of the app asks for"; below "The work handled inside". Example selector use human names only, no Swift class subtitles (put human phrases there). Settings hidden tasks e.g "Find the saved preferences", "Bring preferences forward from an older version", "Use sensible defaults if the saved file cannot be read", "Repair invalid choices", "Save a complete file in one step". PNG hidden tasks human translated. Workflow: start/cancel not equivalent restore! Verify whether cancel() actually restores backups; don't imply cancel universally restores. Replace repeated what-it-isn't disclaimers with one clear illustrative geometry note plus Does not tell you. Each example question should be a high-level architectural question the noncoder could ask, e.g "If we change where settings are saved, how many other parts would need to know?". The issue is technical surface language, not missing facts. Preserve evidence and accurate qualifications. Do not edit other files, delegate, install or change git state. Parent will fix shared height mechanics later. Finish with checks.
