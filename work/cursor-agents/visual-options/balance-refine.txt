Follow-up on 05-balance.html. Parent browser QA found the ribbons overflow horizontally at ~900px iframe width: grid children expand to intrinsic content size and content is clipped. Also most names become ellipses, which doesn't give our noncoder clarity. Refine your owned HTML/notes only. Replace the two crowded stacked ribbon rows with 8 aligned human-labelled rows and an 'Other 36 files' row, each row showing a Size mini bar + visible exact share (%) and an Edited mini bar + commit count. Labels readable and never ellipsized. Two column headings "Share of all code" and "Saved changes" (briefly explain a commit is a saved change in Evidence or one plain footnote). Ensure width calculation uses labelled scale, accurate counts. If size bars scale to max17%, label that scale or use total share with quiet full100% track. Edit bars to 19 available changes; remaining use honest not-comparable. Do not sum other commit counts. Keep selection working with one detail panel; default select Drawing tools so no empty initial state. Remove Compare shape mode if it adds no clear value. This is still the concentration approach; a clear book-like ledger of parts will serve better than tiny multicolored labels. Keep palette and compact desktop height ~850px excluding expanded Evidence, no giant repeated disclaimer paragraphs. At mobile stack row label above pair of bars. Add min-width:0 through grid children, no internal scroll or overflow. Move source paths and jargon to Evidence. Preserve all verified data, no invented risk/quality. Do not delegate or modify other files. Parent fixes shared height mechanics later. Syntax check and finish.
